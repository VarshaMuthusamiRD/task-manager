# Task Manager

A small REST API for managing tasks (Python/FastAPI + SQLite), with a React CRUD frontend.

> This README is a stub during initial scaffolding. It will be filled in with setup
> instructions and example requests/responses in the final documentation slice. See
> `SPEC.md` for the API spec in the meantime.
